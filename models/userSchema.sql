CREATE TABLE Users (
     RegNo int PRIMARY KEY,
     FirstName varchar(255),
     LastName varchar(255),
     Role varchar(255),
     Dept varchar(255),
     Phone varchar(255),
     Email varchar(255),
     Password varchar(255),
     Accept bool
);